GOOGLE_PROJECT_NAME: "GOOGLE_PROJECT_NAME"
MODEL_NAME: "gemini-1.5-pro" # Model name used for the chatbot
USE_LANGCHAIN: "True" # 
DEBUG: "True"
USE_FIRESTORE: "False" # If true, will use the default firestore database of the google project. If false, will use the file system to store the data.

MISTRAL_API_KEY: "xxx"
GOOGLE_API_KEY: "xxx"
OPENAI_API_KEY: XXX
GEMINI_API_KEY: "xxx"
GROQ_API_KEY: "xxx"


# Serveur adresse
LOCAL: True # Boolean to indicate if the model is local or not
MODEL_NAME: "llama3-chatqa:70b" # Model name used for the chatbot
USE_GEMINI: False # (API Key required)
USE_LANGCHAIN: False # (API Key required)
USE_MISTRAL: False # (API Key required)
USE_OPENAI: False # (API Key required)
USE_GROQ: False # (API Key required)

GOOGLE_APPLICATION_CREDENTIALS: path-to-json-file # fill it only if you want to use langchain with a gemini model, and if you are not connected trough the gcloud console using this https://cloud.google.com/sdk/gcloud/reference/auth/application-default/login
